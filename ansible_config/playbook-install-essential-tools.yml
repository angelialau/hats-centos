---

- hosts: target

  tasks:
    - name: Install wget, zip, unzip
      yum:
        name: "{{ item }}"
        state: present
      with_items:
        - wget
        - zip
        - unzip
        - epel-release
      become: yes
      